<div id="works-page" data-user='<%= @user.to_json(only: [:id]) %>' data-works='<%= @works.to_json(only: [:id, :title, :description], include: { work_images: { only: [:image_url, :orientation] } }) %>'>
  <!-- Vueがマウントされます -->
</div>